# Bifrost Docker Stack - Isolated Test Environment
#
# This compose file creates a completely isolated test environment with:
# - Container/volume names derive from $COMPOSE_PROJECT_NAME (set by test.sh
#   per-worktree), so multiple worktrees can run test stacks in parallel
#   without collisions.
# - NO exposed ports (all communication via Docker network)
# - Fresh database for each test run
# - PgBouncer for connection pooling (matches production)
# - SeaweedFS for S3-compatible storage (workspace and files)
#
# NOTE: Ports are NOT exposed to the host, allowing tests to run while
# the development stack is running on the default ports.
#
# Usage: see ./test.sh --help

services:
  # PostgreSQL Database (Test) - with pgvector extension
  # Uses tmpfs so data is NOT persisted between test runs - fresh database each time
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: bifrost_test
      POSTGRES_USER: bifrost
      POSTGRES_PASSWORD: bifrost_test
    # No ports exposed - all access via Docker network
    tmpfs:
      - /var/lib/postgresql/data  # Ephemeral storage - fresh each run
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U bifrost -d bifrost_test"]
      interval: 2s
      timeout: 5s
      retries: 10

  # PgBouncer - Connection Pooler (Test)
  # All services connect through PgBouncer, matching production architecture
  pgbouncer:
    image: edoburu/pgbouncer:latest
    environment:
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: bifrost_test
      DB_USER: bifrost
      DB_PASSWORD: bifrost_test
      POOL_MODE: transaction
      MAX_CLIENT_CONN: 500
      DEFAULT_POOL_SIZE: 10
      MIN_POOL_SIZE: 2
      RESERVE_POOL_SIZE: 2
      AUTH_TYPE: plain
    # No ports exposed - all access via Docker network
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "pg_isready", "-h", "localhost", "-p", "5432", "-U", "bifrost"]
      interval: 2s
      timeout: 5s
      retries: 10

  # RabbitMQ Message Broker (Test)
  rabbitmq:
    image: rabbitmq:3.13-management-alpine
    # Run as uid 100 so .erlang.cookie is created owned by rabbitmq, not root.
    # Docker >= 24 creates the mount point 0755 root:root; the root entrypoint
    # then writes the cookie root-owned 0400 and the uid-100 server can't read
    # it ("Error when reading /var/lib/rabbitmq/.erlang.cookie: eacces").
    # Setting the user fixes the root cause without depending on tmpfs mode
    # bits — important because Docker Desktop/WSL2 does not honour tmpfs
    # uid/gid/mode under `docker compose` (only under `docker run`).
    user: rabbitmq
    environment:
      RABBITMQ_DEFAULT_USER: bifrost
      RABBITMQ_DEFAULT_PASS: bifrost_test
    # No ports exposed - all access via Docker network.
    # Ephemeral storage so test runs never inherit broker state.
    tmpfs:
      - /var/lib/rabbitmq
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "check_running"]
      interval: 5s
      timeout: 10s
      retries: 10

  # Redis (Test)
  redis:
    image: redis:7-alpine
    command: redis-server
    # No ports exposed - all access via Docker network
    tmpfs:
      - /data  # Ephemeral storage - fresh each run
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 2s
      timeout: 5s
      retries: 10

  # SeaweedFS - S3-compatible object storage (Test)
  # Stores workspace files and runtime outputs
  seaweedfs:
    image: chrislusf/seaweedfs:latest
    environment:
      AWS_ACCESS_KEY_ID: bifrost
      AWS_SECRET_ACCESS_KEY: bifrost_test_key
      S3_BUCKET: bifrost-test
    # No ports exposed - all access via Docker network
    tmpfs:
      - /data  # Ephemeral storage - fresh each run

  # Init Container (Test) - Runs migrations and warms module cache before API/workers start
  init:
    image: bifrost-test-api-dev:latest
    build:
      context: .
      dockerfile: api/Dockerfile.dev
    environment:
      BIFROST_DATABASE_URL: postgresql+asyncpg://bifrost:bifrost_test@pgbouncer:5432/bifrost_test
      BIFROST_DATABASE_URL_SYNC: postgresql://bifrost:bifrost_test@pgbouncer:5432/bifrost_test
      BIFROST_REDIS_URL: redis://redis:6379/0
      BIFROST_SECRET_KEY: test-secret-key-for-e2e-testing-must-be-32-chars
      BIFROST_ENVIRONMENT: testing
    volumes:
      - ./api/src:/app/src:ro
      - ./api/shared:/app/shared:ro
      - ./api/alembic:/app/alembic:ro
      - ./api/alembic.ini:/app/alembic.ini:ro
      - ./api/scripts:/app/scripts:ro
    depends_on:
      pgbouncer:
        condition: service_healthy
      redis:
        condition: service_healthy
    command: ["python", "-m", "scripts.init_container"]

  # FastAPI Application (Test) - Only started for E2E tests
  api:
    image: bifrost-test-api-dev:latest
    build:
      context: .
      dockerfile: api/Dockerfile.dev
    environment:
      # Database via PgBouncer (matches production architecture)
      BIFROST_DATABASE_URL: postgresql+asyncpg://bifrost:bifrost_test@pgbouncer:5432/bifrost_test
      BIFROST_DATABASE_URL_SYNC: postgresql://bifrost:bifrost_test@pgbouncer:5432/bifrost_test
      BIFROST_RABBITMQ_URL: amqp://bifrost:bifrost_test@rabbitmq:5672/
      BIFROST_REDIS_URL: redis://redis:6379/0
      BIFROST_SECRET_KEY: test-secret-key-for-e2e-testing-must-be-32-chars
      BIFROST_ENVIRONMENT: testing
      # Browser-facing callbacks use the Playwright loopback client proxy.
      BIFROST_PUBLIC_URL: http://localhost:3000
      # The full E2E suite intentionally reuses session-scoped users. Keep its
      # access tokens valid for the longest supported local/CI run.
      BIFROST_ACCESS_TOKEN_EXPIRE_MINUTES: "120"
      # S3-compatible object storage
      BIFROST_S3_BUCKET: bifrost-test
      BIFROST_S3_ENDPOINT_URL: http://seaweedfs:8333
      BIFROST_S3_ACCESS_KEY: bifrost
      BIFROST_S3_SECRET_KEY: bifrost_test_key
      BIFROST_S3_REGION: us-east-1
      # Enable registration for E2E tests
      BIFROST_ALLOW_REGISTRATION: "true"
      EMBEDDING_ALLOWED_HOSTS: scheduler-fixtures
    # No ports exposed - all access via Docker network (test-runner connects to api:8000)
    volumes:
      - ./api/src:/app/src
      - /app/src/services/app_compiler/node_modules  # preserve npm deps from image
      - /app/src/services/app_bundler/node_modules  # preserve npm deps from image
      - /app/src/services/sdk_package/node_modules  # preserve npm deps from image
      - /app/src/services/sdk_package/sdk_src  # preserve image-baked SDK source (masked by :ro src mount)
      - ./api/shared:/app/shared:ro
      - ./api/bifrost:/app/bifrost:ro
      - ./api/alembic:/app/alembic:ro
      - ./api/alembic.ini:/app/alembic.ini:ro
      - test-mounts:/mounts  # File storage for uploads, branding, etc.
      # Share ONLY the fixture subdir the test-runner stages file:// git repos
      # in, so install/preview-repo can clone them server-side. Do NOT bind the
      # whole LOG_DIR over the api's /tmp/bifrost: the api runs as root and its
      # entrypoint `chown -R`s /tmp/bifrost to the bifrost uid (1000), which
      # would clobber the host-owned LOG_DIR (test-runner.log, test-results.xml,
      # *.log) — on a CI runner whose uid != 1000 the host-side `tee
      # $LOG_DIR/test-runner.log` in run_pytest then fails with EPERM and the
      # step exits 1 even though every test passed. The fixture repos are created
      # by the uid-1000 test-runner, so chown-ing just this subdir to 1000 is a
      # no-op and never touches the LOG_DIR root.
      - ${LOG_DIR:-/tmp/bifrost}/solution-repo-fixtures:/tmp/bifrost/solution-repo-fixtures
    depends_on:
      pgbouncer:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      redis:
        condition: service_healthy
      seaweedfs:
        condition: service_started
      init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health/ready"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 180s
    # Entrypoint handles permissions; run as root so it can fix volume ownership
    user: root
    command: ["uvicorn", "src.main:app", "--host", "0.0.0.0", "--port", "8000"]

  # Background Worker (Test) - Only started for E2E tests
  # Note: Discovery service has been removed - workflow/form extraction
  # now happens at write time in FileStorageService
  worker:
    image: bifrost-test-api-dev:latest
    build:
      context: .
      dockerfile: api/Dockerfile.dev
    environment:
      # Database via PgBouncer (matches production architecture)
      BIFROST_DATABASE_URL: postgresql+asyncpg://bifrost:bifrost_test@pgbouncer:5432/bifrost_test
      BIFROST_DATABASE_URL_SYNC: postgresql://bifrost:bifrost_test@pgbouncer:5432/bifrost_test
      BIFROST_RABBITMQ_URL: amqp://bifrost:bifrost_test@rabbitmq:5672/
      BIFROST_REDIS_URL: redis://redis:6379/0
      BIFROST_SECRET_KEY: test-secret-key-for-e2e-testing-must-be-32-chars
      BIFROST_ENVIRONMENT: testing
      # S3-compatible object storage
      BIFROST_S3_BUCKET: bifrost-test
      BIFROST_S3_ENDPOINT_URL: http://seaweedfs:8333
      BIFROST_S3_ACCESS_KEY: bifrost
      BIFROST_S3_SECRET_KEY: bifrost_test_key
      BIFROST_S3_REGION: us-east-1
      # Lower concurrency for E2E queue position testing (10 executions, 3 concurrent = 7 queued)
      BIFROST_MAX_CONCURRENCY: ${BIFROST_MAX_CONCURRENCY:-3}
      # Worker needs minimal DB connections — Redis-first pattern for agent runs
      BIFROST_DATABASE_POOL_SIZE: "1"
      BIFROST_DATABASE_MAX_OVERFLOW: "2"
      # C1.5: Parity with production hardening
      PIP_NO_CACHE_DIR: "1"
      HOME: /home/bifrost
    volumes:
      - ./api/src:/app/src
      - /app/src/services/app_compiler/node_modules  # preserve npm deps from image
      - /app/src/services/app_bundler/node_modules  # preserve npm deps from image
      - /app/src/services/sdk_package/node_modules  # preserve npm deps from image
      - /app/src/services/sdk_package/sdk_src  # preserve image-baked SDK source (masked by :ro src mount)
      - ./api/shared:/app/shared:ro
      - ./api/bifrost:/app/bifrost:ro
    depends_on:
      pgbouncer:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      seaweedfs:
        condition: service_started
      api:
        condition: service_healthy
    # C1.5: Run as bifrost uid. user: "1000:1000" bypasses gosu so cap_drop: ALL works.
    user: "1000:1000"
    security_opt: ["no-new-privileges:true"]
    cap_drop: ["ALL"]
    command: ["python", "-m", "src.worker.main"]
    profiles:
      - e2e  # Only start in E2E mode

  # Scheduler (Test) - Only started for E2E tests. Every replica is a durable
  # platform-job runner; the trigger leader is elected in PostgreSQL.
  scheduler:
    image: bifrost-test-api-dev:latest
    build:
      context: .
      dockerfile: api/Dockerfile.dev
    environment:
      # Database via PgBouncer (matches production architecture)
      BIFROST_DATABASE_URL: postgresql+asyncpg://bifrost:bifrost_test@pgbouncer:5432/bifrost_test
      BIFROST_DATABASE_URL_SYNC: postgresql://bifrost:bifrost_test@pgbouncer:5432/bifrost_test
      BIFROST_RABBITMQ_URL: amqp://bifrost:bifrost_test@rabbitmq:5672/
      BIFROST_REDIS_URL: redis://redis:6379/0
      BIFROST_SECRET_KEY: test-secret-key-for-e2e-testing-must-be-32-chars
      BIFROST_ENVIRONMENT: testing
      # S3-compatible object storage
      BIFROST_S3_BUCKET: bifrost-test
      BIFROST_S3_ENDPOINT_URL: http://seaweedfs:8333
      BIFROST_S3_ACCESS_KEY: bifrost
      BIFROST_S3_SECRET_KEY: bifrost_test_key
      BIFROST_S3_REGION: us-east-1
      # GitHub E2E test configuration
      GITHUB_TEST_PAT: ${GITHUB_TEST_PAT:-}
      GITHUB_TEST_REPO: ${GITHUB_TEST_REPO:-jackmusick/e2e-test-workspace}
    volumes:
      - ./api/src:/app/src
      - /app/src/services/app_compiler/node_modules  # preserve npm deps from image
      - /app/src/services/app_bundler/node_modules  # preserve npm deps from image
      - /app/src/services/sdk_package/node_modules  # preserve npm deps from image
      - /app/src/services/sdk_package/sdk_src  # preserve image-baked SDK source (masked by :ro src mount)
      - ./api/shared:/app/shared:ro
      - ./api/bifrost:/app/bifrost:ro
      - test-mounts:/mounts
    depends_on:
      pgbouncer:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      redis:
        condition: service_healthy
      seaweedfs:
        condition: service_started
      api:
        condition: service_healthy
      scheduler-fixtures:
        condition: service_healthy
    mem_limit: 1g
    # Entrypoint handles permissions; run as root so it can fix volume ownership
    user: root
    command: ["python", "-m", "src.scheduler.main"]
    profiles:
      - e2e  # Only start in E2E mode

  scheduler-fixtures:
    image: bifrost-test-api-dev:latest
    build:
      context: .
      dockerfile: api/Dockerfile.dev
    command: ["python", "/app/scripts/scheduler_fixture_server.py"]
    volumes:
      - ./api/scripts:/app/scripts:ro
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 2s
      timeout: 2s
      retries: 20
    profiles:
      - e2e

  # Test Runner - Executes pytest inside container with all dependencies
  test-runner:
    image: bifrost-test-api-dev:latest
    build:
      context: .
      dockerfile: api/Dockerfile.dev
    environment:
      # Database via PgBouncer (matches production architecture)
      BIFROST_DATABASE_URL: postgresql+asyncpg://bifrost:bifrost_test@pgbouncer:5432/bifrost_test
      BIFROST_DATABASE_URL_SYNC: postgresql://bifrost:bifrost_test@pgbouncer:5432/bifrost_test
      BIFROST_RABBITMQ_URL: amqp://bifrost:bifrost_test@rabbitmq:5672/
      BIFROST_REDIS_URL: redis://redis:6379/0
      BIFROST_SECRET_KEY: test-secret-key-for-e2e-testing-must-be-32-chars
      BIFROST_ENVIRONMENT: testing
      BIFROST_ALLOW_REGISTRATION: "true"
      # S3-compatible object storage
      BIFROST_S3_BUCKET: bifrost-test
      BIFROST_S3_ENDPOINT_URL: http://seaweedfs:8333
      BIFROST_S3_ACCESS_KEY: bifrost
      BIFROST_S3_SECRET_KEY: bifrost_test_key
      BIFROST_S3_REGION: us-east-1
      # For E2E tests, point to the API container
      TEST_API_URL: http://api:8000
      PYTHONPATH: /app
      # The hardened uid-1000 runner cannot write a root-owned named volume.
      # Keep coverage data beside the already-permissioned per-worktree results.
      COVERAGE_FILE: /tmp/bifrost/.coverage
      # GitHub E2E test configuration (optional - tests skip if not set)
      # Pass from host: GITHUB_TEST_PAT=ghp_xxx ./test.sh e2e
      # Or set in .env.test file
      GITHUB_TEST_PAT: ${GITHUB_TEST_PAT:-}
      GITHUB_TEST_REPO: ${GITHUB_TEST_REPO:-jackmusick/e2e-test-workspace}
      # LLM E2E test configuration (optional - tests skip if not set)
      ANTHROPIC_API_TEST_KEY: ${ANTHROPIC_API_TEST_KEY:-}
      OPENAPI_API_TEST_KEY: ${OPENAPI_API_TEST_KEY:-}
      GENERIC_AI_TEST_KEY: ${GENERIC_AI_TEST_KEY:-}
      GENERIC_AI_BASE_URL: ${GENERIC_AI_BASE_URL:-}
      # Embeddings E2E test configuration (optional - tests skip if not set)
      # Uses OpenAI text-embedding-3-small for vector embeddings
      EMBEDDINGS_AI_TEST_KEY: ${EMBEDDINGS_AI_TEST_KEY:-}
      # Phase 1 hardening is applied to this stack — posture assertions always on
      BIFROST_POSTURE_HARDENED: "1"
    volumes:
      - ./api/src:/app/src
      - /app/src/services/app_compiler/node_modules  # preserve npm deps from image
      - /app/src/services/app_bundler/node_modules  # preserve npm deps from image
      - /app/src/services/sdk_package/node_modules  # preserve npm deps from image
      - /app/src/services/sdk_package/sdk_src  # preserve image-baked SDK source (masked by :ro src mount)
      - ./api/shared:/app/shared:ro
      - ./api/bifrost:/app/bifrost:ro
      - ./api/scripts:/app/scripts:ro  # Scripts for migration utilities
      - ./api/tests:/app/tests:ro
      - ./api/alembic:/app/alembic:ro
      - ./api/alembic.ini:/app/alembic.ini:ro
      - ./api/pytest.ini:/app/pytest.ini:ro
      - ./api/pyproject.toml:/app/pyproject.toml:ro
      - ./api/pyrightconfig.json:/app/pyrightconfig.json:ro
      - ./api/Dockerfile.dev:/app/api/Dockerfile.dev:ro
      - ./.github/workflows:/app/.github/workflows:ro  # CI topology contract tests
      - ./test.sh:/app/test.sh:ro
      - ./docker-compose.test.yml:/app/docker-compose.test.yml:ro  # Read-only —
                                      # the harness guard in
                                      # tests/unit/test_compose_test_harness.py
                                      # asserts the api service never re-binds
                                      # the host LOG_DIR root over /tmp/bifrost.
      - ./client/src:/client/src:ro  # Read-only — the drift test in
                                      # tests/unit/test_platform_names_match_runtime.py
                                      # parses app-code-runtime.ts to detect new platform exports.
      - ./client/e2e:/app/client/e2e:ro  # Browser smoke selection is part of
                                      # the repository-controlled CI safety contract.
      - ./client/playwright.config.ts:/app/client/playwright.config.ts:ro
      - ./client/nginx.conf:/app/client/nginx.conf:ro  # Production routing contract.
      - ./client/Dockerfile:/app/client/Dockerfile:ro  # Production asset-mode contract.
      - ./.claude/skills:/.claude/skills:ro  # Read-only — the drift test in
                                      # tests/unit/test_platform_api_docs.py reads
                                      # bifrost-build/platform-api.md to enforce docs
                                      # cover every PLATFORM_EXPORT_NAMES entry.
      # Host-mounted for test results (junitxml) — per-worktree, driven by
      # LOG_DIR (set by test.sh to /tmp/bifrost-$COMPOSE_PROJECT_NAME) so two
      # worktrees running in parallel don't clobber each other's results.
      - ${LOG_DIR:-/tmp/bifrost}:/tmp/bifrost
    working_dir: /app
    depends_on:
      pgbouncer:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      redis:
        condition: service_healthy
      seaweedfs:
        condition: service_started
      init:
        condition: service_completed_successfully
    # C1.5: Run as bifrost uid so posture tests introspect a hardened context.
    # user: "1000:1000" bypasses gosu so cap_drop: ALL works.
    user: "1000:1000"
    security_opt: ["no-new-privileges:true"]
    cap_drop: ["ALL"]
    # Default command runs all tests except E2E
    command: ["pytest", "tests/", "--ignore=tests/e2e/", "-v"]
    profiles:
      - test  # Only start when running tests

  # Node 26 matches actions/setup-node in CI. The builder target compiles the
  # production client before this runner executes TypeScript, ESLint, and the
  # complete Vitest suite against the same source snapshot.
  client-check-runner:
    image: bifrost-test-client-check:latest
    environment:
      # Respect Vitest's worker override on shared development hosts while
      # retaining the configured default when no override is supplied.
      VITEST_MAX_WORKERS: ${VITEST_MAX_WORKERS:-}
    build:
      context: ./client
      dockerfile: Dockerfile
      target: builder
      args:
        VITE_BIFROST_VERSION: test
    command: ["sh", "-c", "npm run tsc && npm run lint && npm test"]
    profiles:
      - client-check

  # =============================================================================
  # Client E2E Testing Services (started with --profile client)
  # =============================================================================

  # Production client frontend used by Playwright.
  #
  # Browser tests exercise the same compiled assets and nginx routing shipped
  # in the real client image. A Vite dev server made full-suite timing depend
  # on cold, on-demand module transforms and never tested the production build.
  client:
    image: bifrost-test-client-e2e:latest
    build:
      context: ./client
      dockerfile: Dockerfile
      target: production
      args:
        VITE_BIFROST_VERSION: test
    depends_on:
      api:
        condition: service_healthy
    networks:
      default:
        aliases:
          - allowed-origin
          - blocked-origin
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O /dev/null http://127.0.0.1:80/"]
      interval: 5s
      timeout: 5s
      retries: 30
    profiles:
      - client  # Only start when --profile client is used

  # Playwright E2E test runner
  # Runs browser tests against the client service
  playwright-runner:
    image: bifrost-test-playwright:latest
    build:
      context: ./client
      dockerfile: Dockerfile.playwright
    environment:
      # Chromium grants WebCrypto to localhost HTTP just as it does to deployed
      # HTTPS. The runner proxies these loopback origins to the client service.
      TEST_BASE_URL: http://localhost:3000
      TEST_ALLOWED_ORIGIN: http://localhost:3001
      TEST_BLOCKED_ORIGIN: http://localhost:3002
      TEST_API_URL: http://api:8000
      CI: "true"
    volumes:
      - ./client/e2e:/app/e2e
      - ./client/playwright.config.ts:/app/playwright.config.ts:ro
      - ./client/package.json:/app/package.json:ro
      - ./client/tsconfig.json:/app/tsconfig.json:ro
      - ./client/tsconfig.app.json:/app/tsconfig.app.json:ro
      - ./client/tsconfig.node.json:/app/tsconfig.node.json:ro
      - ./client/playwright-results:/app/playwright-results
    depends_on:
      client:
        condition: service_healthy
    profiles:
      - client  # Only start when --profile client is used

# Named volumes for test data - ephemeral within test session
volumes:
  test-mounts:  # File storage for uploads, branding, etc.
