services:
  # Development Azurite - for running the app
  azurite:
    image: mcr.microsoft.com/azure-storage/azurite:latest
    ports:
      - "10000:10000" # Blob
      - "10001:10001" # Queue
      - "10002:10002" # Table
    volumes:
      - ./azurite:/data

  # Testing Azurite - for running tests (separate data, different ports)
  azurite-test:
    image: mcr.microsoft.com/azure-storage/azurite:latest
    ports:
      - "10100:10100" # Blob
      - "10101:10101" # Queue
      - "10102:10102" # Table
    command: "azurite --blobHost 0.0.0.0 --blobPort 10100 --queueHost 0.0.0.0 --queuePort 10101 --tableHost 0.0.0.0 --tablePort 10102 --loose --inMemoryPersistence"
    # Note: --inMemoryPersistence means tests start with clean slate each time
    # Note: Uses different internal ports (10100-10102) so devcontainer can distinguish from dev azurite

  functions:
    build:
      context: ./api
      dockerfile: Dockerfile
    # ports:
    #   - "7072:80" # Azure Functions runtime
    #   - "5678:5678" # Debugpy (when ENABLE_DEBUGGING=true)
    environment:
      - AzureWebJobsStorage=DefaultEndpointsProtocol=http;AccountName=devstoreaccount1;AccountKey=Eby8vdM02xNOcqFlqUwJPLlmEtlCDXJ1OUzFT50uSRZ6IFsuFq2UVErCz4I6tq/K1SZFPTOtr/KBHBeksoGMGw==;BlobEndpoint=http://azurite:10000/devstoreaccount1;QueueEndpoint=http://azurite:10001/devstoreaccount1;TableEndpoint=http://azurite:10002/devstoreaccount1;
      - AzureWebJobsSecretStorageType=files
      - ENABLE_DEBUGGING=${ENABLE_DEBUGGING:-true}
      - AZURE_FUNCTIONS_ENVIRONMENT=Development
      - WEBSITE_AUTH_ENCRYPTION_KEY=C0sm0Rk3y567890123456789012345678901234567890123456789012345
      - AZURE_KEY_VAULT_URL=${AZURE_KEY_VAULT_URL}
      - AZURE_CONFIG_DIR=/root/.azure # This allows the Functions container to use the Azure CLI credentials from the host
    volumes:
      - ~/.azure:/root/.azure # Mount Azure CLI credentials
      - ./api:/home/site/wwwroot # Live code reload
      - ./api/workspace:/workspace # Workspace directory (local dev)
      - ./api/tmp:/tmp # Temporary storage (local dev)
    depends_on:
      - azurite

  swa:
    build:
      context: ./client
      dockerfile: Dockerfile
    ports:
      - "4280:4280" # SWA CLI
    environment:
      - API_URL=http://functions:80
      - ENTRA_CLIENT_ID=${ENTRA_CLIENT_ID}
      - ENTRA_CLIENT_SECRET=${ENTRA_CLIENT_SECRET}
    volumes:
      - ./client:/app # Live code reload
      - /app/node_modules # Prevent overwriting node_modules (use container's version)
    depends_on:
      - functions
