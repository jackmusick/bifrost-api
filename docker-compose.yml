services:
  azurite:
    image: mcr.microsoft.com/azure-storage/azurite:latest
    ports:
      - "10000:10000" # Blob
      - "10001:10001" # Queue
      - "10002:10002" # Table
    command: "azurite --blobHost 0.0.0.0 --queueHost 0.0.0.0 --tableHost 0.0.0.0 --loose"
    volumes:
      - azurite-data:/data

  functions:
    build:
      context: ./workflows
      dockerfile: Dockerfile
    ports:
      - "7072:80" # Azure Functions runtime
      - "5678:5678" # Debugpy (when ENABLE_DEBUGGING=true)
    environment:
      - AzureWebJobsStorage=DefaultEndpointsProtocol=http;AccountName=devstoreaccount1;AccountKey=Eby8vdM02xNOcqFlqUwJPLlmEtlCDXJ1OUzFT50uSRZ6IFsuFq2UVErCz4I6tq/K1SZFPTOtr/KBHBeksoGMGw==;BlobEndpoint=http://azurite:10000/devstoreaccount1;QueueEndpoint=http://azurite:10001/devstoreaccount1;TableEndpoint=http://azurite:10002/devstoreaccount1;
      - AzureWebJobsSecretStorageType=files
      - ENABLE_DEBUGGING=${ENABLE_DEBUGGING:-true}
      - AZURE_FUNCTIONS_ENVIRONMENT=Development
      - WEBSITE_AUTH_ENCRYPTION_KEY=C0sm0Rk3y567890123456789012345678901234567890123456789012345
    volumes:
      - ./workflows:/home/site/wwwroot # Live code reload
      - ~/GitHub/BifrostIntegrationsWorkspace:/workspace # Simulates Azure Files mount for workspace files
      - ~/Downloads/tmp:/tmp # Simulates Azure Files mount for temporary storage
    depends_on:
      - azurite

volumes:
  azurite-data:
