name: Tests and Coverage

on:
    push:
        branches: [main]
    pull_request:
        branches: [main]
    workflow_dispatch:
jobs:
    test-api:
        runs-on: ubuntu-latest
        name: API Tests
        # Only run in upstream repo, not forks
        if: github.event.repository.fork == false

        steps:
            - uses: actions/checkout@v4

            - name: Set up Python 3.11
              uses: actions/setup-python@v4
              with:
                  python-version: "3.11"

            - name: Start Azurite
              run: |
                  npx --yes azurite --silent --inMemoryPersistence &
                  # Wait for Azurite to be ready by checking table service
                  for i in {1..30}; do
                    if curl -s http://127.0.0.1:10002/devstoreaccount1?comp=properties > /dev/null 2>&1; then
                      echo "Azurite is ready!"
                      break
                    fi
                    if [ $i -eq 30 ]; then
                      echo "Timeout waiting for Azurite"
                      exit 1
                    fi
                    echo "Waiting for Azurite to start... ($i/30)"
                    sleep 1
                  done
              shell: bash

            - name: Install dependencies
              run: |
                  cd api
                  python -m pip install --upgrade pip
                  pip install -r requirements.txt
                  pip install pytest pytest-cov pytest-asyncio

            - name: Seed Azurite with test data
              working-directory: ./api
              run: |
                  export PYTHONPATH=.
                  python seed_data.py
              env:
                  AzureWebJobsStorage: UseDevelopmentStorage=true

            - name: Run tests with coverage
              working-directory: ./api
              run: |
                  npm run test:ci
              env:
                  PYTHONPATH: .
                  AzureWebJobsStorage: UseDevelopmentStorage=true
                  FUNCTIONS_WORKER_RUNTIME: python
                  AZURE_FUNCTIONS_ENVIRONMENT: Testing
                  AZURE_KEY_VAULT_URL: https://mock-keyvault.vault.azure.net/

            - name: Upload coverage to Codecov
              uses: codecov/codecov-action@v4
              with:
                  files: ./api/coverage.xml
                  flags: api
                  name: api-coverage
                  token: ${{ secrets.CODECOV_TOKEN }}
