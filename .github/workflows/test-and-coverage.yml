name: Tests and Coverage

on:
  push:
    branches: [main]
    paths:
      - "api/**"
      - "requirements.txt"
      - "test.sh"
      - "docker-compose.test.yml"
      - ".github/workflows/test-and-coverage.yml"
  pull_request:
    branches: [main]
    paths:
      - "api/**"
      - "requirements.txt"
      - "test.sh"
      - "docker-compose.test.yml"
      - ".github/workflows/test-and-coverage.yml"
  workflow_dispatch:

jobs:
  # Unit and Integration tests (fast, with coverage)
  test-api:
    if: |
      github.repository == 'jackmusick/bifrost-api'
    runs-on: ubuntu-latest
    name: Unit & Integration Tests

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Run tests with coverage
        run: |
          chmod +x test.sh
          ./test.sh --coverage

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          files: ./coverage.xml
          flags: api
          name: api-coverage
          token: ${{ secrets.CODECOV_TOKEN }}

  # E2E tests (full stack, slower)
  test-e2e:
    if: |
      github.repository == 'jackmusick/bifrost-api'
    runs-on: ubuntu-latest
    name: E2E Tests
    needs: test-api  # Run after unit/integration tests pass

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Run E2E tests
        run: |
          chmod +x test.sh
          ./test.sh --e2e
