# Environment variables for local development
#
# Copy this file to .env and fill in your values
# .env is gitignored and will not be committed
#
# NOTE: Variables defined here will be loaded by Azure Functions automatically
# via python-dotenv in function_app.py. You don't need to duplicate them in
# local.settings.json unless they're specifically required by Azure Functions runtime.

# Azure Key Vault (for secrets in local dev)
# If not set, secrets will be stored in Azure Table Storage
AZURE_KEY_VAULT_URL=

# Azure Web PubSub (for real-time log streaming)
WebPubSubConnectionString=

# Workspace and temp directories
# BIFROST_WORKSPACE_LOCATION=/path/to/workspace
# BIFROST_TEMP_LOCATION=/path/to/tmp

# Debugging
# ENABLE_DEBUGGING=true  # Uncomment to enable debugpy on port 5678

# Azure Storage (usually set in local.settings.json for Azure Functions runtime)
# AzureWebJobsStorage=UseDevelopmentStorage=true
