# Environment variables for Docker Compose local development
#
# Copy this file to .env and fill in your values
# .env is gitignored and will not be committed

# Azure App Registration for SWA Authentication (GetRoles)
# Get these from Azure Portal > App Registrations
ENTRA_CLIENT_ID=your-app-registration-client-id
ENTRA_CLIENT_SECRET=your-app-registration-client-secret

# Azure Key Vault (for secrets in local dev - optional)
# If not set, uses local storage
AZURE_KEY_VAULT_URL=

# Additional local dev settings
# ENABLE_DEBUGGING=true  # Uncomment to enable debugpy on port 5678
