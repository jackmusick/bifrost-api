# Settings Acceptance Inventory

Scope: current platform and account settings routes in the design-system-modernization worktree. This reconciles source, backend/API contracts where visible from browser tests, and checked-in browser/component evidence. It is not a final-candidate claim and does not say the full settings inventory is complete.

## Route Shells

| Route | Current panels | Existing evidence | Gaps |
| --- | --- | --- | --- |
| `/settings` and `/settings/:tab` | `ai`, `ai-embeddings`, `ai-chat`, `ai-memory`, `ai-instructions`, `ai-usage`, `mcp`, `github`, `sso`, `workflow-keys`, `branding`, `maintenance` from `client/src/pages/Settings.tsx`. | `client/src/pages/Settings.test.tsx` exercises unknown-tab recovery, desktop/mobile settings navigation, draft retention for visited mocked panels, inactive control hiding, and section expand/collapse. | Shell tests mock every real panel, so they prove routing/chrome only. Panel acceptance must come from the per-panel rows below. |
| `/user-settings` and `/user-settings/:tab` | `basic-info`, `security`, `connections`, `preferences`, `developer` from `client/src/pages/UserSettings.tsx`. | `client/src/pages/UserSettings.test.tsx` covers unknown-tab recovery, tab switching, draft retention for mocked panels, and inactive controls hidden. | Shell tests do not exercise real account settings controls. |

## Platform Settings Panels

| Route/tab | Supported user actions in current source | Browser/e2e evidence read | Component/service/backend evidence read | True remaining gaps |
| --- | --- | --- | --- | --- |
| `/settings/ai` Models | Manage reusable AI provider connections and profiles; verify provider credentials; assign model profiles to runtime slots; merge profiles; set chat defaults. | `client/e2e/ai-model-settings.admin.spec.ts` drives the routed UI with intercepted `/api/admin/ai/*` responses: creates a provider, verifies it, creates profiles, assigns one, sets defaults, merges profiles, and captures screenshots. | `client/src/pages/settings/AIModelSettings.test.tsx` and `ModelCapabilityEditor.test.tsx` cover loaded state, create/edit flows, failed provider verification, assignment selection, merge behavior, default handling, and capability overrides. | Browser evidence is routed but intercepted, not live provider authentication. Destructive provider/profile delete paths are lower-layer only. |
| `/settings/ai-embeddings` Embeddings | Select provider connection and embedding model; save embedding config; confirm reindex when needed; configure image/video generation model fields in child component. | `client/e2e/ai-model-settings.admin.spec.ts` visits `/settings/ai-embeddings`, intercepts provider/model/config APIs, selects a provider/model, clicks `Save embeddings`, and asserts the saved payload. | `AIEmbeddingSettings.test.tsx`, `GenerationModelSettings.test.tsx`, and memory service tests cover disabled model until provider selection, save payloads, provider-change clearing, reindex confirmation retry, and generation model filtering/inputs. | No live-browser proof for reindex confirmation failure/retry. Generation-model controls are component-level only. |
| `/settings/ai-chat` Chat Instructions | Edit and save global agentless chat instructions; retry failed read/save while preserving draft. | `client/e2e/chat-instructions-acceptance.admin.spec.ts` contains `CHAT-INSTRUCTIONS-01 saves and reloads default chat instructions`; parent reports it passed as part of the 49-journey ledger. | `AIBehaviorSettings.test.tsx` covers read/save error recovery and draft preservation for behavior/instruction settings. | Targeted browser save/reload proof now exists for the default chat instructions path. Save-error retry remains component-level proof; full release-candidate gates remain pending. |
| `/settings/ai-memory` Memory | Toggle platform memory on/off and retry unavailable settings. | `client/e2e/memory.admin.spec.ts` navigates directly to `/settings/ai-memory`, reads/restores current backend settings, disables then enables memory through the UI switch, verifies enabled UI state, and attaches screenshot evidence. Nightly audit records the repaired targeted memory run as passing. | `MemorySettings.test.tsx` covers enable and failed-load retry. Memory service tests cover platform and user settings request contracts. | The core platform memory happy path has browser evidence. Error/retry remains lower-layer only. |
| `/settings/ai-instructions` Default MCP Instructions | Edit/save global required instructions; retry failed read/save; organization-scoped variant is reused outside Settings when given an organization id. | `client/e2e/memory.admin.spec.ts` navigates directly to `/settings/ai-instructions`, edits `Global Instructions editor`, saves, then calls `/mcp` `bifrost_get_required_instructions` and asserts global plus organization instructions resolve. | `RequiredInstructionsSettings.test.tsx` covers global and organization-scoped save, failed read retry before editing, scope clearing during load, and failed-save draft retention. Service tests cover global/org URLs and update requests. | Core global browser path is covered. Organization-specific settings are covered through `/organizations`, not the platform Settings tab. Error/retry remains lower-layer only. |
| `/settings/ai-usage` Usage & Pricing | Add/edit/delete per-model pricing rows and retry read errors. | `client/e2e/ai-pricing-acceptance.admin.spec.ts` contains `adds, edits, reloads, and deletes a unique model pricing record`; parent reports it passed as part of the 49-journey ledger. | `AIUsageSettings.test.tsx` surfaces missing rates and opens the pricing form. `ModelPricingList.test.tsx`, `PricingEditDialog.test.tsx`, and `PricingDeleteDialog.test.tsx` cover create/edit/delete mechanics. | Targeted browser lifecycle proof now exists for one owned pricing row. Failure retries, provider/model matrix variants, and release-candidate gates remain separate. |
| `/settings/mcp` MCP | Toggle MCP access; pick allowed and blocked tools; view/copy connection details; save/reset config; retry config/tool reads. | `client/e2e/mcp-settings-acceptance.admin.spec.ts` visits `/settings/mcp`, verifies the `MCP connection details` region, toggles enablement, selects allowed/blocked real tool ids from `/api/mcp/tools`, saves, reloads, asserts persisted `/api/mcp/config`, resets to defaults, reloads, and restores prior config. Nightly audit records the sixth settings/resource run passing. | `client/src/pages/settings/MCP.test.tsx` covers initial config read failure, cached config with tool read failure, failed-save draft retention, and reset-dialog failure. Backend unit/router tests from the audit cover platform-admin configuration inventory when MCP is disabled and preserve runtime filtering for callers. | Primary settings lifecycle is covered. Copy-to-clipboard affordance and visual responsive states are not separately browser-proven here. This tab does not manage external MCP server templates. |
| `/settings/github` GitHub | Enter/validate PAT; select repository and branch; create repository; configure/disconnect saved repo; retry config/repo/branch reads. | `client/e2e/github-settings-acceptance.admin.spec.ts` visits `/settings/github` and drives token validation, repository/branch selection, create-repository dialog, configure, reload connected summary, and disconnect. All `/api/github/*` calls are intercepted by the spec. Nightly audit records this intercepted GitHub settings journey passing. | `GitHub.test.tsx` covers validate/select/configure payload, create-repo payload and branch loading, configured summary, and disconnect confirmation. `GitHubCreateRepositoryDialog.test.tsx` covers create failure focus and bounded footer. | Browser evidence is UI-contract evidence only; it intentionally does not prove live GitHub credentials, real repository creation, or provider network behavior. |
| `/settings/sso` Authentication | Configure/delete Microsoft, Google, and OIDC providers; edit configured providers; save preferred sign-in behavior; retry SSO config reads. | `client/e2e/sso-settings-acceptance.admin.spec.ts` visits `/settings/sso`, creates Google SSO through the real UI, edits it, reloads persisted configured state, saves preferred provider with `Prefer SSO on login` off, reloads preference, removes Google config, and asserts backend config cleared. Nightly audit records SSO settings passing. `preferred-sso.admin.spec.ts` separately proves unauthenticated login redirect behavior after API seeding. | `OAuth.test.tsx` covers saving a configured provider as preferred first attempt. OAuth service tests cover preference mutation invalidation. | Google provider primary lifecycle is covered. Microsoft/OIDC-specific UI fields and provider test buttons are not browser-proven. External provider login is covered only by API-seeded redirect behavior, not by settings UI. |
| `/settings/workflow-keys` Workflow Keys | Create workflow API key; reveal/copy generated key; list after reload; refresh keys/workflow metadata; revoke key. | `client/e2e/workflow-keys-acceptance.admin.spec.ts` registers an endpoint-enabled workflow fixture, visits `/settings/workflow-keys`, creates a key, verifies the reveal dialog without logging the raw key, copies it via clipboard, reloads, verifies persisted metadata from `/api/workflow-keys`, revokes the key, refreshes, and asserts the list is empty. Nightly audit records the sixth repair run passing. | `WorkflowKeys.test.tsx` covers cached keys during read failure and cached rows when workflow metadata refresh fails. `WorkflowKeyDialogs.test.tsx` covers create/reveal/revoke dialog behavior. | Primary create/copy/reload/revoke lifecycle is covered. Failure/retry states remain component-level. |
| `/settings/branding` Branding | Update product name, primary color, terminology, square logo, rectangle logo; reset color or all branding; retry read errors. | `client/e2e/branding-terminology.admin.spec.ts` visits `/settings/branding`, updates primary color and terminology through the form, reloads, verifies persisted `/api/branding`, verifies renamed nouns on Apps/Agents/Forms, returns to reset color, and asserts the primary action settles to default theme color. Nightly audit records Branding passing after strengthened reset assertion. `design-foundations.admin.spec.ts` separately verifies routed branding colors in shell/action contrast with intercepted `/api/branding`. | `Branding.test.tsx` covers failed initial load/retry, default controls, loaded custom color preview, reset semantics, terminology save semantics, cached branding on refresh failure, draft preservation, and save disabling. `LogoDropZone.test.tsx` covers shared upload mechanics. | Terminology and primary-color lifecycle are browser-covered. Product name and square/rectangle logo upload/reset are not covered by this settings browser spec; logo upload has shared component evidence and separate entity-logo browser evidence outside Settings. |
| `/settings/maintenance` Maintenance | Run documentation and app-dependency maintenance actions, export all, import all, reimport all, and inspect action results. | Existing browser evidence is intercepted/synthetic in the design modernization settings review docs; no live-browser maintenance action spec is inventoried here. | `client/src/pages/settings/Maintenance.test.tsx` covers fixed queue order for selected documentation/app-dependency actions, visible last results, reimport completion errors with retryability, Export All service wiring, and Import All dialog wiring. Parent reports the Maintenance component tests passed in the latest targeted run. | Browser proof for real maintenance action execution/export/import/reimport remains missing. Current durable proof is component/service contract coverage plus lower-layer import/export behavior. |

## Account Settings Panels

| Route/tab | Supported user actions in current source | Browser/e2e evidence read | Component/service/backend evidence read | True remaining gaps |
| --- | --- | --- | --- | --- |
| `/user-settings/basic-info` Basic Info | Retry profile load; edit display name; upload/remove avatar; set/change password; show/hide password; retry failed saves. | `client/e2e/profile-acceptance.user.spec.ts` visits `/user-settings/basic-info`, reads the seeded member profile through `/api/profile`, edits `Name`, waits for the PATCH, polls `/api/profile` for the saved value, reloads the page, verifies the saved name and disabled `Saved` button, attaches a screenshot, and restores the original name. Ledger records this as `PROFILE-01`. | `BasicInfo.test.tsx` covers initial load retry, failed name-save retry, password draft retention, show/hide password, set-password success without extra profile fetch, matching-password validation, and existing-password change payload. `LogoDropZone.test.tsx` covers shared avatar upload mechanics. | Display-name save/reload is browser-covered. Avatar upload/remove and set/change password through the real route remain missing primary browser journeys. |
| `/user-settings/security` Security | Retry MFA/passkey reads; set up/verify/remove TOTP; acknowledge/copy/download/regenerate recovery codes; add/delete passkeys; handle unsupported passkeys. | `client/e2e/account-security-acceptance.admin.spec.ts` creates an isolated user, completes login and MFA setup through real auth APIs, opens `/user-settings/security` in that user's browser context, registers a persisted passkey using Chromium virtual authenticator, verifies `/auth/passkeys`, reloads, removes the passkey through UI, verifies deletion, and cleans up the user. Nightly audit records this passkey journey passing. | `Security.test.tsx`, `SecurityMfaDialogs.test.tsx`, and `SecurityPasskeyDialogs.test.tsx` cover cached passkeys/read retry, failed authenticator setup retry/focus, add/delete passkey wiring, pending/error/focus states for MFA and passkey dialogs. | Passkey primary lifecycle is browser-covered. The settings UI still lacks browser acceptance for TOTP setup/removal, recovery-code copy/download/regeneration, and unsupported-passkey states. |
| `/user-settings/connections` Connections | Refresh MCP definitions/server names/personal credential status; connect/reconnect via OAuth popup; disconnect; retry partial read/connect/disconnect failures. | `client/e2e/mcp-personal-connection-acceptance.admin.spec.ts` creates server/connection metadata through API, opens `/user-settings/connections`, starts the user OAuth popup against the scheduler fixture provider, waits for persisted `/api/me/mcp-connections`, reloads connected state, disconnects through UI, and verifies credential removal. Nightly audit records the sixth settings/resource run passing. | `UserMCPConnections.test.tsx` covers refresh of all three queries, cached rows with retry, connect pending through popup dismissal, popup blocked/closed/message handling, disconnect failure retry, and action-state behavior. | Primary personal OAuth connect/reload/disconnect is covered. Responsive card/table layout and partial-failure visual states remain lower-layer or review evidence. |
| `/user-settings/preferences` Preferences | Toggle personal memory preference; list saved memories; remove saved memory; retry failed toggle/delete/load. | `client/e2e/memory.admin.spec.ts` visits `/user-settings/preferences`, toggles user memory off/on, creates saved memory through MCP, reloads, verifies persisted saved memory text, removes it through UI, and cleans up. Nightly audit records the repaired targeted memory run as passing. | `Preferences.test.tsx` covers opt-out, saved markdown rendering, toggle save retry, duplicate toggle guard, remove confirmation, failed load retry, failed delete retry, duplicate delete guard, and focus restoration. Memory service tests cover user memory settings requests. | Primary memory preference and saved-memory management are browser-covered. Failure/retry states remain lower-layer only. |
| `/user-settings/developer` Developer | Copy CLI setup commands; download SDK tarball; open documentation link. | No checked-in e2e spec found that visits `/user-settings/developer`. | `Developer.test.tsx` verifies the uv-compatible CLI install text and SDK download URL. Source review shows clipboard copy feedback/error state in each setup step. | Missing browser proof for clipboard success/failure feedback, SDK download response, external docs link behavior, and responsive command layout. |

## Cross-Cutting Evidence Notes

- Browser tests that route/intercept their own APIs are useful UI contract evidence, but they do not prove live third-party provider behavior. This applies most clearly to GitHub settings, AI model settings, artifact retention cleanup, and the design-foundations branding color check.
- `client/e2e/memory.admin.spec.ts` is now valid route-specific evidence for `/settings/ai-memory`, `/settings/ai-instructions`, `/user-settings/preferences`, organization instructions, and MCP required-instructions resolution. The older concern that it only visited `/settings/ai` is obsolete.
- `client/e2e/preferred-sso.admin.spec.ts` proves login redirect behavior after API seeding. It supplements `sso-settings-acceptance.admin.spec.ts`; it does not replace settings-form evidence.
- `client/e2e/mcp-settings-acceptance.admin.spec.ts` covers platform MCP server access configuration. It is distinct from external MCP server/connection management under `/mcp-servers` and from personal connections under `/user-settings/connections`.
- `client/e2e/github-settings-acceptance.admin.spec.ts` intentionally intercepts all GitHub backend calls. It should be counted for UI persistence/reload/disconnect behavior, not for real GitHub credentials or repository creation.
- Route-string-only searches are insufficient for this inventory. Specs such as `profile-acceptance.user.spec.ts` may prove a settings default or tab through semantic UI actions and ledger IDs even when the filename does not include the tab name.

## Clear Remaining Gaps

- Platform AI: `/settings/ai-chat` and `/settings/ai-usage` now have targeted browser happy paths. AI model destructive delete, pricing failure retries, provider/model matrix variants, and embedding reindex retry remain lower-layer or pending.
- Platform connections/security: MCP settings primary lifecycle is covered; GitHub is intercepted-only; SSO covers Google but not Microsoft/OIDC-specific provider forms or provider test buttons.
- Platform branding/maintenance: Branding color and terminology are covered, but product name and logo settings are not. Maintenance retention is covered with intercepted APIs; docs indexing, dependency scan, reimport, export/import-all lack primary browser evidence.
- Account settings: Basic Info has browser evidence for display-name save/reload, but not avatar. Security has passkey browser evidence; `client/e2e/account-onboarding-acceptance.admin.spec.ts` contains TOTP/recovery-code and password-change journeys, but parent is still repairing account test setup sessions, so those remain pending here. Developer has component evidence only. Connections and Preferences have primary browser evidence, with partial-failure/responsive cases remaining below e2e.
